// Complementing a Strand of DNA

fn transcription(dna_string: &str) {
    /*
    This function returns the reverse complement of a DNA sequence.

    Input: a DNA string
    Prints: the reverse complement string
    */
    let rev_string = reverse_string(dna_string); // reverses the dna string
    let comp_string = ; // replace bases with their complements
    print!("{}", comp_string);
}

fn main() {
    transcription(); // enter DNA string here
}
